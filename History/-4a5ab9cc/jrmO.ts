function add(n1: number, n2: number) {
  return n1 + n2;
}

function example(arg1: number, arg2: number): string {
  // function body

  return "somethng"; // result should be a string type
}

example(1, 2);
