import { ReactNode } from 'react';

function Heading({ as, children }: { as: string; children: ReactNode }) {
  const h2 = 'font-semibold text-[24px]';

  return <div></div>;
}

export default Heading;
