const today = new Temporal.Now.plainDateISO();
console.log(today.toString()); // Output: "2024-07-02"

function des(x) {
  console.log(`${x} times`);
}

class des {
  name;
  constructor(n) {
    this.name = n;
  }
}
